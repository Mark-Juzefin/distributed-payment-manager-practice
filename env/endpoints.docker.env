# Docker Endpoints (container network)
# URLs for docker-compose services

PG_URL=postgres://postgres:secret@db:5432/payments?sslmode=disable
KAFKA_BROKERS=kafka:29092
OPENSEARCH_URLS=http://opensearch-node1:9200
SILVERGATE_BASE_URL=http://silvergate-mock:8080
API_BASE_URL=http://api-service:3000
